package me.itzusman.velotab.universal;

/**
 * Esta clase sirve para documentar que el JAR Universal contiene todas las clases
 * de Paper, Velocity y BungeeCord. Cada plataforma cargara su clase Main correspondiente
 * segun lo definido en sus respectivos archivos de metadatos (plugin.yml, velocity-plugin.json, bungee.yml).
 */
public class PlatformLoader {
    // El JAR Universal se construye sombreando (shading) los otros modulos o 
    // simplemente teniendo todas las clases en el mismo classpath.
}
